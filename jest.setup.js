// Jest setup file
// Runs before each test suite

// Increase test timeout for platform-specific operations
jest.setTimeout(10000);

// Suppress console warnings during tests (optional)
// global.console = {
//   ...console,
//   warn: jest.fn(),
// };
